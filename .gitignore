# Agentify
.agent/
.agentify/
# Ignorar solo cambios locales activos (evita subir borradores)
openspec/changes/*
# Permitir persistir el archivo histórico (archive)
!openspec/changes/archive/
.claude/
__pycache__/
